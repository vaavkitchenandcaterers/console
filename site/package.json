{
  "name": "vaav-kitchen-pro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "sync:chrome": "node ../tools/sync-chrome.mjs",
    "build:menu": "npm run sync:chrome && node ../tools/build-menu-pages.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
